@model IEnumerable<OnlineShopping.ViewModel.CompletedOrdersViewModel>

@{
    ViewBag.Title = "Orders History";
}

<h2>Your Orders History</h2>
@if (Model.Count() > 0 && Model != null)
{
    <table class="table">
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.OrderCreatedOn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderDeliveredOn)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.OrderCreatedOn)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TotalAmount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OrderDeliveredOn)
                </td>
                <td>
                </td>
            </tr>
        }

    </table>

}
else
{
    <h2>You have not purchased any products!</h2>
}
@Html.ActionLink("Back", "ProductsList", "Product")